spring:
  application:
    name: andres
  datasource:
    url: jdbc:h2:mem:andres;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
